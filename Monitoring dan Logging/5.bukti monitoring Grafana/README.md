# Bukti Monitoring Grafana

Dashboard Grafana sudah diprovisioning dengan nama folder/dashboard `muhamadadha`.

URL lokal:
`http://localhost:3000`

Login default dari `docker-compose.yml`:
- username: `admin`
- password: `admin`

Screenshot panel dashboard disimpan di folder ini.
